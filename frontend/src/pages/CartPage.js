import React from 'react';
import { Container, Typography } from '@mui/material';

function CartPage() {
  return (
    <Container>
      <Typography variant="h4">Shopping Cart</Typography>
      {/* Add content for the cart here */}
    </Container>
  );
}

export default CartPage;
